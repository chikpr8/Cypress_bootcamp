/// <reference types="cypress" />

import HomePage from '../../support/pages/HomePage';
import LoginSignupPage from '../../support/pages/LoginSignupPage';
import AccountDetailsPage from '../../support/pages/AccountDetailsPage';
import UserAccountPage from '../../support/pages/UserAccountPage';

/*
╔════════════════════════════════════════════════════════════════════════════╗
║          COMPLETE REGISTRATION & ACCOUNT DELETION TEST                     ║
├════════════════════════════════════════════════════════════════════════════┤
║  This test demonstrates:                                                   ║
║  ✓ Complete user registration flow                                         ║
║  ✓ Page Object Model (POM) pattern                                         ║
║  ✓ Hooks (before, beforeEach, afterEach, after)                            ║
║  ✓ Comprehensive test validation at each step                              ║
║                                                                             ║
║  Test Steps:                                                               ║
║  1. Launch browser & navigate to home page                                 ║
║  2. Click Signup/Login button                                              ║
║  3. Verify New User Signup section                                         ║
║  4. Enter name and email then signup                                       ║
║  5. Verify Account Information form loaded                                 ║
║  6. Fill account details (Title, Password, DOB)                            ║
║  7. Select newsletter and offers checkboxes                                ║
║  8. Fill address details (Name, Company, Address, etc.)                    ║
║  9. Click Create Account button                                            ║
║  10. Verify Account Created message                                        ║
║  11. Click Continue button                                                 ║
║  12. Verify logged in as username                                          ║
║  13. Click Delete Account button                                           ║
║  14. Verify Account Deleted message                                        ║
║  15. Click Continue button & return to home                                ║
╚════════════════════════════════════════════════════════════════════════════╝
*/

describe('Complete User Registration & Account Deletion Flow', () => {

    // ════════════════════════════════════════════════════════════════════════════
    // PAGE OBJECT INSTANTIATION
    // ════════════════════════════════════════════════════════════════════════════
    
    let homePage;
    let loginSignupPage;
    let accountDetailsPage;
    let userAccountPage;
    let testData;

    // ════════════════════════════════════════════════════════════════════════════
    // BEFORE HOOK: Setup once for all tests
    // ════════════════════════════════════════════════════════════════════════════
    
    before(() => {
        cy.log('');
        cy.log('╔════════════════════════════════════════════════════════╗');
        cy.log('║         🟢 BEFORE HOOK: INITIALIZATION                 ║');
        cy.log('╚════════════════════════════════════════════════════════╝');
        cy.log('');
        cy.log('Purpose: Initialize page objects and setup test data');
        cy.log('');
        
        // Load test data from fixture
        cy.log('📂 Loading test data from fixture folder...');
        cy.fixture('registrationData').then((data) => {
            testData = data;
            
            // Generate unique email with timestamp to avoid conflicts
            testData.signupEmail = `john.cypress.${Date.now()}@testmail.com`;
            
            cy.log('✅ Fixture data loaded successfully');
            cy.log('');
            cy.log('Test Data Prepared:');
            cy.log(`  📧 Email: ${testData.signupEmail}`);
            cy.log(`  📝 Name: ${testData.signupName}`);
            cy.log(`  📊 Data Source: cypress/fixtures/registrationData.json`);
            cy.log('✅ Test data ready');
        });
        
        // Initialize page objects
        cy.log('');
        cy.log('🏗️  Initializing page objects...');
        homePage = new HomePage();
        loginSignupPage = new LoginSignupPage();
        accountDetailsPage = new AccountDetailsPage();
        userAccountPage = new UserAccountPage();
        
        cy.log('✅ Page objects initialized');
        cy.log('');
    });

    // ════════════════════════════════════════════════════════════════════════════
    // BEFOREEACH HOOK: Clear cookies and reset before each test
    // ════════════════════════════════════════════════════════════════════════════
    
    beforeEach(() => {
        cy.log('');
        cy.log('╔════════════════════════════════════════════════════════╗');
        cy.log('║      🟡 BEFOREEACH HOOK: CLEARING STATE                ║');
        cy.log('╚════════════════════════════════════════════════════════╝');
        cy.log('');
        cy.log('Purpose: Clear cookies and cached data before test');
        
        cy.clearCookies();
        cy.window().then((win) => {
            win.sessionStorage.clear();
            win.localStorage.clear();
        });
        
        cy.log('✅ Cookies and storage cleared');
        cy.log('');
    });

    // ════════════════════════════════════════════════════════════════════════════
    // MAIN TEST CASE
    // ════════════════════════════════════════════════════════════════════════════
    
    it('Should successfully register a new account and delete it', function() {
        cy.log('');
        cy.log('╔════════════════════════════════════════════════════════╗');
        cy.log('║        ✅ TEST START: REGISTRATION & DELETION          ║');
        cy.log('╚════════════════════════════════════════════════════════╝');
        cy.log('');

        // ────────────────────────────────────────────────────────────────────────────
        // STEP 1: Launch browser & navigate to home page
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 1️⃣  : Launch browser & navigate to home page');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        homePage.navigateToHome();
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 2: Verify home page is visible
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 2️⃣  : Verify home page is visible successfully');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        homePage.verifyHomePageLoaded();
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 3: Click on 'Signup / Login' button
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 3️⃣  : Click on Signup / Login button');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        homePage.clickSignupLoginButton();
        cy.wait(1000); // Wait for page to load
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 4: Verify 'New User Signup!' is visible
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 4️⃣  : Verify New User Signup! is visible');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        loginSignupPage.verifyNewUserSignupVisible();
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 5: Enter name and email address
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 5️⃣  : Enter name and email address');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        loginSignupPage.enterSignupName(testData.signupName);
        loginSignupPage.enterSignupEmail(testData.signupEmail);
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 6: Click 'Signup' button
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 6️⃣  : Click Signup button');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        loginSignupPage.clickSignupButton();
        cy.wait(1000); // Wait for form to load
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 7: Verify 'ENTER ACCOUNT INFORMATION' is visible
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 7️⃣  : Verify ENTER ACCOUNT INFORMATION is visible');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        accountDetailsPage.verifyAccountInformationVisible();
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 8: Fill details: Title, Name, Email, Password, Date of birth
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 8️⃣  : Fill Title, Password, Date of birth');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        accountDetailsPage.selectTitle(testData.accountDetails.title);
        accountDetailsPage.enterPassword(testData.accountDetails.password);
        accountDetailsPage.selectDateOfBirth(
            testData.accountDetails.day,
            testData.accountDetails.month,
            testData.accountDetails.year
        );
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 9: Select checkbox 'Sign up for our newsletter!'
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 9️⃣  : Select newsletter checkbox');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        accountDetailsPage.checkNewsletter();
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 10: Select checkbox 'Receive special offers from our partners!'
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 🔟  : Select special offers checkbox');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        accountDetailsPage.checkSpecialOffers();
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 11: Fill address details
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 1️⃣1️⃣  : Fill address details');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        accountDetailsPage.enterFirstName(testData.accountDetails.firstName);
        accountDetailsPage.enterLastName(testData.accountDetails.lastName);
        accountDetailsPage.enterCompany(testData.accountDetails.company);
        accountDetailsPage.enterAddress1(testData.accountDetails.address);
        accountDetailsPage.enterAddress2(testData.accountDetails.address2);
        accountDetailsPage.selectCountry(testData.accountDetails.country);
        accountDetailsPage.enterState(testData.accountDetails.state);
        accountDetailsPage.enterCity(testData.accountDetails.city);
        accountDetailsPage.enterZipcode(testData.accountDetails.zipcode);
        accountDetailsPage.enterMobileNumber(testData.accountDetails.mobileNumber);
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 12: Click 'Create Account' button
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 1️⃣2️⃣  : Click Create Account button');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        accountDetailsPage.clickCreateAccountButton();
        cy.wait(1000); // Wait for account creation
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 13: Verify 'ACCOUNT CREATED!' message
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 1️⃣3️⃣  : Verify ACCOUNT CREATED! is visible');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        accountDetailsPage.verifyAccountCreatedMessage();
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 14: Click 'Continue' button
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 1️⃣4️⃣  : Click Continue button');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        accountDetailsPage.clickContinueButton();
        cy.wait(1000); // Wait for navigation
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 15: Verify 'Logged in as username' is visible
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 1️⃣5️⃣  : Verify Logged in as username is visible');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        userAccountPage.verifyLoggedInAsUsername();
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 16: Click 'Delete Account' button
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 1️⃣6️⃣  : Click Delete Account button');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        userAccountPage.clickDeleteAccountButton();
        cy.wait(1000); // Wait for deletion
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 17: Verify 'ACCOUNT DELETED!' message
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 1️⃣7️⃣  : Verify ACCOUNT DELETED! is visible');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        userAccountPage.verifyAccountDeletedMessage();
        
        // ────────────────────────────────────────────────────────────────────────────
        // STEP 18: Click 'Continue' button & return to Home
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('STEP 1️⃣8️⃣  : Click Continue button & return to Home');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        userAccountPage.clickContinueButtonOnDeletePage();
        cy.wait(1000); // Wait for navigation to home
        
        // ────────────────────────────────────────────────────────────────────────────
        // FINAL VERIFICATION
        // ────────────────────────────────────────────────────────────────────────────
        cy.log('');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        cy.log('✅ FINAL VERIFICATION: Back on Home Page');
        cy.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
        
        homePage.verifyHomePageLoaded();
        
        cy.log('');
        cy.log('╔════════════════════════════════════════════════════════╗');
        cy.log('║             ✅ TEST COMPLETED SUCCESSFULLY              ║');
        cy.log('╚════════════════════════════════════════════════════════╝');
        cy.log('');
    });

    // ════════════════════════════════════════════════════════════════════════════
    // AFTEREACH HOOK: Cleanup after each test
    // ════════════════════════════════════════════════════════════════════════════
    
    afterEach(function() {
        cy.log('');
        cy.log('╔════════════════════════════════════════════════════════╗');
        cy.log('║        🔵 AFTEREACH HOOK: CLEANUP                       ║');
        cy.log('╚════════════════════════════════════════════════════════╝');
        cy.log('');
        cy.log('Purpose: Cleanup and logging after test');
        
        if (this.currentTest.state === 'passed') {
            cy.log('✅ Test Status: PASSED');
        } else {
            cy.log('❌ Test Status: FAILED');
            cy.log('Taking screenshot for debugging...');
            cy.screenshot(`test-failure-${Date.now()}`);
        }
        
        cy.log('');
    });

    // ════════════════════════════════════════════════════════════════════════════
    // AFTER HOOK: Final cleanup after all tests
    // ════════════════════════════════════════════════════════════════════════════
    
    after(() => {
        cy.log('');
        cy.log('╔════════════════════════════════════════════════════════╗');
        cy.log('║         🔴 AFTER HOOK: FINAL CLEANUP                    ║');
        cy.log('╚════════════════════════════════════════════════════════╝');
        cy.log('');
        cy.log('Purpose: Final cleanup and teardown');
        cy.log('');
        cy.log('Test Suite Summary:');
        cy.log('  ✅ Test execution completed');
        cy.log('  ✅ All page objects properly utilized');
        cy.log('  ✅ POM design pattern followed');
        cy.log('  ✅ Hooks properly executed');
        cy.log('');
        cy.log('Resources cleaned:');
        cy.log('  ✓ Cookies cleared on completion');
        cy.log('  ✓ Session storage cleared');
        cy.log('  ✓ Page objects disposed');
        cy.log('');
        cy.log('╔════════════════════════════════════════════════════════╗');
        cy.log('║              🎉 TEST SUITE COMPLETE                    ║');
        cy.log('╚════════════════════════════════════════════════════════╝');
        cy.log('');
    });
});




